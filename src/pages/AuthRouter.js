import React from 'react';
import LandingPage from './LandingPage';

/**
 * Auth Router - Landing page ve auth sayfaları arası geçiş
 * Artık tüm routing LandingPage içinde yapılıyor
 */

const AuthRouter = () => {
  return <LandingPage />;
};

export default AuthRouter;
